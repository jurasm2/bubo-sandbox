{if $images}
    <div class="img-container hp-slider slide-me">
        {foreach $images as $image}
            {dump $image}
            <img src="{$image->getPath(1)}" alt="{$image->getDescription('title')}" title="{$image->getDescription('title')}" />
        {/foreach}
    </div>
{/if}